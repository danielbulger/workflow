server:
  port: 5000
spring:
  datasource:
    url: jdbc:mariadb://172.17.0.2:3306/workflow
    username: root
    password: root
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MariaDBDialect
        format_sql: true
        id:
          new_generator_mappings: false